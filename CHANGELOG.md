# Changelog

<!-- <START NEW CHANGELOG ENTRY> -->

## 0.2.1 会话日志与平台接入（2026-08-04）

- 会话 finalize 后立即生成可读的 `operation_log.json` 和
  `process_log.md`；只在 AI 分析真实 `ready` 后生成
  `analysis_log.json`。
- 左侧“本次日志”固定按操作、过程、AI 分析排序，支持文件名/
  “查看”打开只读页签及经认证下载；文件夹入口移至高级诊断。
- 新增白名单会话日志 list/view/download API，拒绝路径穿越、符号链接、
  非常规文件和超大行内查看。
- 将时长标签明确为“有效观察时长（证据覆盖）”，说明页面离开与
  运行耗时的排除规则，且不将停顿直接当作心理状态。
- 提供 BLUEDOT JupyterLab 4 基础镜像接入 Dockerfile、持久化目录、
  运行时密钥注入和动态 `/notebook_<uuid>/` 前缀验收说明。

## 0.2.0 Pilot 加固（2026-07-29）

- 封闭 Python 文件执行路径：等待 Contents Manager 校验并拒绝 Jupyter 根目录
  外的符号链接、目录和非本地 Manager。
- 区分数据采集完成与 AI 结论生成；未配置 AI 时不再显示结果卡或复核表单。
- AI 配置增加字段级安全错误提示和带二次确认的 Key 清除入口。
- 为 Notebook/Python 监控器增加直接自动化测试与文件级覆盖率门槛。
- “运行当前 Python 文件”加入命令面板、运行菜单和文件右键菜单，运行结果中文化。
- 修复行为区间时间戳与持续时间的毫秒级取时偏差：新数据由最终时间戳
  统一计算持续时间，历史数据兼容最多 2 毫秒漂移，较大矛盾仍拒绝分析。
- 监控侧栏显示题目方案要求的有效观察时间、页面离开时间和实时进度；页面离开
  不计入有效观察，达到最低要求后会明确提示。
- 有效观察不足时首次停止会说明将得到“数据不足”，教师可继续监控或明确选择
  仍要停止；停止时会结算最后一段前台空闲，避免界面进度与后端分析不一致。
- 分析任务的 `queued`、`running`、`ready`、`partial`、`error` 状态全部使用
  中文显示，并继续与结果区的数据不足或失败原因分开呈现。
- 按交付决定保留 `requires-python >=3.10`；本轮实际验证环境为 Python 3.12。

<!-- <END NEW CHANGELOG ENTRY> -->
