# 启动说明

本文面向 `myextension 0.3.0` Pilot 的 wheel 部署。普通安装不需要 Node.js。

## 环境要求

- macOS、Linux 或 Windows 10/11
- Python 3.12
- [uv](https://docs.astral.sh/uv/)
- 可安装 `jupyterlab==4.6.1` 和 `jupyter-server==2.20.0`
- 交付文件 `dist/myextension-0.4.0-py3-none-any.whl`

命令必须在本项目根目录运行。不要复用来源不明的 Python 环境。

## macOS / Linux 安装

```bash
uv venv --python 3.12
uv pip install --python .venv/bin/python \
  "jupyterlab==4.6.1" \
  "jupyter-server==2.20.0"
uv pip install --python .venv/bin/python \
  dist/myextension-0.4.0-py3-none-any.whl
```

检查前端和服务端扩展：

```bash
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jupyter labextension list
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jupyter server extension list
```

两项都应显示 `myextension` 已启用。

启动并保留鉴权：

```bash
PATH="$PWD/.venv/bin:$PATH" \
  .venv/bin/python -m jupyter lab --no-browser
```

终端会输出带 `?token=...` 的本机 URL。复制该完整 URL 到浏览器；不要把 token
URL 发到聊天、截图或共享文档。

## Windows PowerShell 安装

```powershell
uv venv --python 3.12
uv pip install --python ".venv\Scripts\python.exe" `
  "jupyterlab==4.6.1" `
  "jupyter-server==2.20.0"
uv pip install --python ".venv\Scripts\python.exe" `
  ".\dist\myextension-0.4.0-py3-none-any.whl"
```

检查扩展：

```powershell
& ".\.venv\Scripts\jupyter.exe" labextension list
& ".\.venv\Scripts\jupyter.exe" server extension list
```

启动方式二选一：

```powershell
.\start_project.bat
```

或：

```powershell
& ".\.venv\Scripts\python.exe" -m jupyter lab --no-browser
```

`start_project.bat` 只在当前窗口启动服务器，不关闭 token/password，也不会自动
打开一个未知的 8888 页面。请从终端输出复制本次服务器的完整 token URL。

## 端口冲突

Jupyter 默认尝试 8888；该端口被占用时通常会自动选择下一端口。也可以明确指定：

macOS / Linux：

```bash
PATH="$PWD/.venv/bin:$PATH" \
  .venv/bin/python -m jupyter lab --no-browser --port=8889
```

Windows PowerShell：

```powershell
& ".\.venv\Scripts\python.exe" -m jupyter lab --no-browser --port=8889
```

不要因为端口冲突而使用 `--ServerApp.token=` 或
`--ServerApp.password=`。也不要直接打开未确认属于本次进程的
`http://localhost:8888/lab`。

## 首次使用

1. 用终端输出的 token URL 打开 JupyterLab。
2. 打开左侧“行为分析”。
3. 点击“创建题目考核方案”，输入题目；可选填希望考察的知识点，一行一个。题目
   会先自动保存为部分草稿，但未确认知识点和测试前不能发布。
4. 需要 AI 推荐时，先展开“AI 服务配置”，填写 Base URL、模型和 API Key；
   未配置 AI 也可以手工添加知识点和测试。
5. 检查并编辑推荐知识点，确认后继续；再检查测试草稿、关联知识点和期望结果。
6. 确认知识点与测试后发布方案。当前测试仅作为题目设计草稿保存，不会自动执行。
7. 选择已发布方案，勾选本次采集用途确认，再点击“开始监控”。
8. 在 Notebook 中使用原生运行按钮；在 `.py` 文件中从顶部“运行”菜单、命令
   面板或文件编辑器右键菜单选择“运行当前 Python 文件”。
9. 编辑、运行、制造一次错误、修改并再次运行。
10. 点击“停止监控”，等待剩余上传和 finalize。左侧“本次日志”会立即将 `operation_log.json` 和 `process_log.md` 变为可查看/下载。
11. `analysis_log.json` 在等待期间显示“正在分析…”，只在真实 `ready` 后可打开；`partial/error` 显示“分析未完成”，不会生成假的成功日志。
12. 已配置 AI 时查看证据并提交教师复核；AI 结论只是辅助判断，教师复核单独追加
    保存。AI 分析进入 `ready`/`partial` 或教师复核成功保存后，服务端会刷新同一个
    `training_record.json`。
13. 未配置外部 AI 时也会自动生成记录：刚停止时 `ai_analysis` 可以为空；后台检查后，
    最低观察量不足会写入不调用外部模型的 coverage-only `ready` 结果；需要模型但未配置
    时，会话分析状态变为 `partial`，训练记录随状态刷新，但公开 `ai_analysis` 仍为空，
    不会伪造模型结论。源码快照和行为事件继续保留。
14. “高级数据”只用于兼容文件核查和排障，不是日常查看入口。

监控默认关闭；每次新会话都要重新选择/确认。普通流程不依赖双击日志文件。

“有效观察时长（证据覆盖）”统计监控期间的代码输入、删除、粘贴及页面活动时的动作间停顿。页面离开不计入；运行事件会写入日志，但运行耗时不计入该时长。达到门槛只表示行为证据覆盖足够，与日志生成或 AI 分析等待无关。

外部 AI Base URL 必须使用 HTTPS；只有运行在本机回环地址的服务允许使用 HTTP。

BLUEDOT 平台的镜像安装、持久化、密钥注入和验收方法见 [BLUEDOT 平台接入说明](docs/2026-08-04-bluedot-platform-integration.md)。
本版课堂学生镜像候选目录是 `deploy/bluedot/release-0.4.0/`。运行时必须设置 `JUPYTERLAB_BEHAVIOR_AUDIT_PLATFORM_MODE=student`、真实课堂同步服务 HTTPS 地址和 `/workspace/result` 持久卷；学生未手动提交时，由服务端按下课后 15 分钟的证据截止时间自动收口。该候选包尚未构建或部署到真实 BAMS 基础镜像。

刷新页面可恢复关闭前已进入 IndexedDB/BAMS 的事件；关闭页面后不再采集新操作。当前插件只在学生 JupyterLab 中生成本地简报，不会自动发送到 FinColab 教师端。

真实日志、会话目录和自动生成的训练记录可能包含学生代码与教学数据，不得提交到版本库或发送到
外部服务、聊天或共享位置。当前版本仍是本地单用户 Pilot，不适用于共享 JupyterHub 的多租户正式部署。

## 本次日志与本地诊断边界

日常使用时直接点击“本次日志”里的文件名、“查看”或“下载”，无需进入 Finder/Explorer。三份可读产物位于 `sessions/<session_id>/logs/`：`operation_log.json`、`process_log.md`、`analysis_log.json`。“打开日志文件夹”仅保留在“高级数据”中，它打开运行 Jupyter Server 的机器，远程平台不会打开教师电脑的文件夹。

## API Key 安全

- Key 只保存在运行 Jupyter Server 的机器，不保存在浏览器设置或 wheel 中。
- 在支持 POSIX 文件权限的平台，配置文件使用 `0600`。
- 不要提交、复制、截图或共享包含 Key 的配置文件，也不要把 `log/` 发送给他人。
- 停止使用当前 provider 时，在“AI 服务配置”中点击“清除已保存 Key”并确认。
- 如果旧版本曾保存过同一个 Key，迁移到 0.3.0 后应在 provider 侧轮换旧 Key，
  再在“AI 服务配置”中保存新 Key。

## 故障排查

### 两项扩展检查没有同时通过

使用安装 wheel 的同一虚拟环境：

```bash
jupyter server extension enable myextension --sys-prefix
```

然后彻底停止旧 JupyterLab 并重新运行启动命令。

### 侧栏仍是旧界面

确认浏览器连接的是刚才 token URL 对应的服务器，关闭旧标签页，重新启动服务器后
硬刷新页面。不要仅凭 8888 端口判断进程。

### 找不到“创建题目考核方案”

先打开左侧“行为分析”，在未选择方案时使用侧栏中的创建按钮；也可以打开
JupyterLab 命令面板，搜索“创建题目考核方案”。如果仍显示旧的“模板/维度”
界面，说明当前服务器加载的还是旧 wheel，请彻底停止服务器，重新安装新构建的
wheel 后再启动。

### 停止后分析较慢

上传与分析是分阶段执行的。先查看待上传数，再查看“已排队/分析中/ready”状态，
必要时点击“刷新状态”。若显示“重试上传/结束”，优先重试，避免丢弃尚未确认的
本地记录。若显示“数据采集完成，尚未进行 AI 分析”，采集已成功，只是当前没有
AI 结论；配置 AI 服务后重试即可。

### Python 文件找不到运行入口

确认当前活动文件以 `.py` 结尾。可从顶部“运行”菜单或命令面板选择“运行当前
Python 文件”，也可在文件编辑器中右键运行。该命令不会对非 `.py` 文件启用。

## 从当前源码构建新 wheel

只有开发或重新打包本次修改时需要 Node.js。以下操作会重建前端和 wheel，但不会
自动安装或启动：

```bash
uv venv --python 3.12
uv sync --extra dev --extra test --no-install-project
PATH="$PWD/.venv/bin:$PATH" uv pip install --python .venv/bin/python -e ".[dev,test]"
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm install --immutable
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm build:prod
PATH="$PWD/.venv/bin:$PATH" uv build --wheel
```

随后在目标环境安装 `dist/` 中新生成的 wheel，并彻底重启 JupyterLab。不要只
覆盖源码目录后继续使用已经运行的旧服务器。

如果目标环境已经安装过同版本 `0.3.0`，需要显式覆盖旧 wheel。

macOS / Linux：

```bash
uv pip install --python .venv/bin/python --reinstall \
  dist/myextension-0.4.0-py3-none-any.whl
```

Windows PowerShell：

```powershell
uv pip install --python ".venv\Scripts\python.exe" --reinstall `
  ".\dist\myextension-0.4.0-py3-none-any.whl"
```
