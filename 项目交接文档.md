# 编程行为监控分析插件项目交接文档

交接日期：2026-08-04  
项目版本：`0.2.1`  
项目形态：JupyterLab 4 本地单用户 Pilot  
交付状态：**0.2.1 源码已完成本次日志体验和 BLUEDOT 镜像接入资产；本地全量回归、wheel 构建与哈希在本文第 9 节记录。真实 AI、BLUEDOT 真实镜像/工作台和 Windows 真机仍待验收，不得直接标记为客户最终验收版。**

## 1. 交接结论

本项目已经形成“教师创建题目考核方案—学生编程行为采集—会话结束后分析—教师复核—自动生成训练记录”的本地闭环，并完成以下交付能力：

- 教师可按题目创建、确认并发布知识点和测试草稿；
- AI 知识点与测试建议要求使用简体中文，纯英文建议会被拒绝，不进入前端列表；
- 学生明确确认后才能开始监控，停止监控后才统一分析；
- 左侧“本次日志”固定显示操作、过程和 AI 分析三份日志；
- 前两份在会话 finalize 后立即生成，AI 日志仅在真实 `ready` 后生成；
- 日志可在 JupyterLab 中直接查看或下载，文件夹入口仅保留为高级本机诊断；
- 保留 AI 结果与教师复核的职责边界，不自动评分或判断知识掌握程度。

本轮已处理两项新近手动验收问题：

1. 结构化 AI 响应偶发在首次请求达到输出上限，导致测试建议第一次失败、第二次才成功；
2. 会话 AI 分析失败时，前端仍可能显示“部分结果”和“查看 0 条证据”，容易被误解为已经完成了有效分析。

两项修复先进入当时的 0.2.0 wheel，现已包含在当前 0.2.1 wheel。隔离浏览器验证保证一次操作内从首次长度截断恢复；AI 分析失败且无可用结论时，结果区和任务状态均不显示“部分结果”，也不渲染占位卡。设计与实际验证见 [结构化 AI 响应截断恢复设计](docs/superpowers/specs/2026-08-04-structured-ai-truncation-recovery-design.md)、[结构化 AI 截断恢复与失败态验证记录](docs/2026-08-04-structured-ai-truncation-recovery-verification.md) 和 [0.2.0 隔离 wheel 验收记录](docs/2026-08-04-isolated-wheel-acceptance.md)。本轮没有调用真实/付费 AI，未重启或修改当前 `127.0.0.1:8899` 预览；wheel 仅安装到临时隔离目录进行验收。

本次日志与平台接入的批准口径见 [会话日志与 BLUEDOT 接入设计](docs/superpowers/specs/2026-08-04-session-log-experience-platform-integration-design.md) 和 [BLUEDOT 平台接入说明](docs/2026-08-04-bluedot-platform-integration.md)。

## 2. 项目位置与交付产物

项目根目录：

```text
/Users/sxh/编程行为监控分析插件_交付版_20260727
```

主要产物：

| 项目               | 内容                                                               |
| ------------------ | ------------------------------------------------------------------ |
| npm/Python 包名    | `myextension`                                                      |
| JupyterLab 插件 ID | `myextension:plugin`                                               |
| 当前版本           | `0.2.1`                                                            |
| 当前 wheel         | `dist/myextension-0.2.1-py3-none-any.whl`                          |
| 当前 wheel SHA-256 | `7138965244a5f71b9307ca89c5585cdb58aa1206a2ba1a0c13e57147bbeecf98` |
| 回退 wheel         | `dist/myextension-0.1.0-py3-none-any.whl`                          |
| 回退 wheel SHA-256 | `48da402c5bc7229aee2683ae69c8f3a63af85bf8e3aa38555756ea53ea2b5fc7` |
| Windows 启动脚本   | `start_project.bat`                                                |
| 启动文档           | `启动说明.md`                                                      |
| 功能与数据边界     | `项目说明.md`、`README.md`                                         |
| OpenAPI 契约       | `docs/openapi/myextension-v1.yaml`                                 |

注意：当前目录不是 Git 仓库，不能通过 commit 标识版本。本次使用 wheel 文件名、SHA-256 和验证记录作为交付凭证。

## 3. 产品定位与业务边界

本工具面向教师的编程教学观察场景。它采集编辑、删除、粘贴、运行成功或失败、停顿、页面可见性和切换等客观事件，并在完整会话结束后提供基于证据的教学观察。

系统输出不应被用于：

- 自动评分、成绩、排名、处分或淘汰；
- 判断学生“已掌握”或“未掌握”某项知识；
- 能力、人格、心理状态或其他高风险诊断；
- 将“运行成功”直接等同于答案正确；
- 将“停顿”直接解释为犹豫或能力不足。

测试草稿当前只用于教师确认题目覆盖范围，系统会保存测试方案，但不会自动执行测试，也不会依据测试自动判定掌握情况。

## 4. 核心业务流程

```text
教师输入题目和可选考察重点
→ AI 推荐知识点，教师采用、修改或手工添加
→ 教师确认知识点
→ AI 生成测试草稿，教师检查并确认
→ 发布不可变的题目考核方案
→ 学生选择已发布方案并确认采集用途
→ 开始监控
→ 在 Notebook 或 Python 文件中编辑、运行、报错和调试
→ 侧栏解释“有效观察时长（证据覆盖）”的统计口径
→ 达到最低证据覆盖要求后停止监控
→ 上传剩余记录并完成会话
→ 服务端创建一次会话分析任务
→ 立即生成 operation_log.json 和 process_log.md
→ AI ready 后生成 analysis_log.json
→ 展示会话结果卡和教师复核入口
→ AI 分析或教师复核更新时刷新同一训练记录
```

## 5. 安装与启动

### 5.1 macOS / Linux

在项目根目录执行：

```bash
uv venv --python 3.12
uv pip install --python .venv/bin/python \
  "jupyterlab==4.6.1" \
  "jupyter-server==2.20.0"
uv pip install --python .venv/bin/python \
  dist/myextension-0.2.1-py3-none-any.whl

PATH="$PWD/.venv/bin:$PATH" .venv/bin/jupyter labextension list
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jupyter server extension list
PATH="$PWD/.venv/bin:$PATH" .venv/bin/python -m jupyter lab --no-browser
```

终端会输出包含 token 的完整访问地址。请复制完整地址到浏览器，不要只打开不含 token 的 `/lab` 地址，也不要关闭认证。

### 5.2 Windows PowerShell

在项目根目录执行：

```powershell
uv venv --python 3.12
uv pip install --python ".venv\Scripts\python.exe" `
  "jupyterlab==4.6.1" `
  "jupyter-server==2.20.0"
uv pip install --python ".venv\Scripts\python.exe" `
  ".\dist\myextension-0.2.1-py3-none-any.whl"

& ".\.venv\Scripts\jupyter.exe" labextension list
& ".\.venv\Scripts\jupyter.exe" server extension list
.\start_project.bat
```

启动脚本会保留 JupyterLab 的 token 认证。浏览器访问时同样应使用启动窗口输出的完整 token URL。

### 5.3 安装验收

`jupyter labextension list` 与 `jupyter server extension list` 都应显示 `myextension` 已启用。若服务端扩展未启用，可执行：

```bash
jupyter server extension enable myextension --sys-prefix
```

随后彻底停止并重新启动 JupyterLab。

## 6. 客户演示流程

建议用一段包含正常运行、一次故意报错和修复后成功运行的 Python 示例完成演示。

### 6.1 创建并发布方案

1. 打开 JupyterLab 左侧“行为分析”。
2. 点击“创建题目考核方案”。
3. 输入题目，例如“编写一个成绩统计函数，返回人数、平均分、最高分、最低分和及格率，并处理空列表与越界成绩”。
4. 获取 AI 知识点建议，确认所有客户可见说明均为中文；也可直接手工新增或修改。
5. 确认知识点。
6. 获取测试草稿，检查输入、预期输出和关联知识点；需要时手工修改。
7. 确认测试并发布。说明该阶段只保存测试方案，不会执行测试。

### 6.2 采集编程行为

1. 回到左侧“行为分析”，选择刚发布的方案。
2. 勾选本次采集用途确认。
3. 点击“开始监控”。
4. 在 Notebook 或 `.py` 文件中编写并运行示例程序。
5. 故意制造一次错误，例如使用不存在的变量或返回错误字段；运行后再修复。
6. 再次运行并得到成功结果。
7. 保持 JupyterLab 在前台，等待“有效观察时长（证据覆盖）”达到最低要求。该时长只统计输入、删除、粘贴及页面活动时的动作间停顿；页面离开和运行耗时不计入。
8. 点击“停止监控”，等待上传、结束和分析状态更新。

### 6.3 查看结果与本次日志

1. 查看会话结果卡和数据质量说明。
2. 若 AI 分析成功，展开证据并核对对应行为事件。
3. 在“教师复核”中确认或修正观察结论。
4. 在左侧“本次日志”直接打开/下载 `operation_log.json` 和 `process_log.md`，说明它们不等 AI。
5. AI 分析成功后打开/下载 `analysis_log.json`；若显示“分析未完成”，不得当作成功日志。

演示时不要将 AI 结论描述为评分或知识掌握结论。若出现第 9 节所述两项 P0 问题，应停止对外验收演示并先完成修复。

## 7. AI 配置与中文输出保证

AI Base URL、模型和 API Key 在 Jupyter Server 所在机器配置。API Key 只保存在服务端配置中，不应出现在前端提示词、截图、日志、交接文件或聊天中。

配置路径依次使用 `JUPYTERLAB_BEHAVIOR_AUDIT_AI_CONFIG_PATH`、`JUPYTERLAB_BEHAVIOR_AUDIT_LOG_DIR` 下的配置文件、BLUEDOT 当前工作区 `/workspace/code/.behavior-audit/.ark_ai_config.json`，最后才是普通本地默认位置。工作区回退是当前单用户试点的便捷方式，不等于将 Key 编译进插件；其跨容器持久性取决于 BLUEDOT 的 `/workspace/code` 持久化策略。

当前中文输出策略：

- 知识点和测试建议的系统提示明确要求客户可见自然语言使用简体中文；
- 知识点的客户可见自然语言字段必须包含中文字符；
- 测试建议名称必须包含中文字符；
- 纯英文响应会失败关闭，不进入前端建议列表；
- Python 标识符、测试输入、期望输出和必要技术术语允许保留原文。

相关实现与验证记录：

- `myextension/assessment_assistant.py`
- [AI 建议中文输出设计](docs/superpowers/specs/2026-08-04-chinese-ai-suggestions-design.md)
- [AI 建议中文输出验证记录](docs/2026-08-04-chinese-ai-suggestions-verification.md)

不再使用当前 provider 时，应在界面中点击“清除已保存 Key”，并按单位安全规范决定是否在 provider 侧轮换密钥。

## 8. 本次日志和训练记录

左侧“本次日志”是日常入口，按固定顺序显示：

```text
sessions/<session_id>/logs/operation_log.json
sessions/<session_id>/logs/process_log.md
sessions/<session_id>/logs/analysis_log.json
```

需要注意：

- `operation_log.json` 和 `process_log.md` 在会话 finalize 后立即可看/下载；
- 这两份非 AI 日志首次生成后冻结，后续 AI 完成或教师复核不会改写其内容；
- `analysis_log.json` 仅在分析 `ready` 后可看/下载；
- AI 任务刚进入 `ready` 而文件尚在落盘的短暂窗口仍显示“正在分析…”，页面会有限重试，不会固定成错误态；
- 文件名和“查看”会在 JupyterLab 主区打开只读页签，不依赖 Finder/Explorer；
- “打开日志文件夹”只保留在“高级数据”中作本地诊断；
- `training_record.json` 仍在会话停止后自动生成；
- AI 分析进入 `ready`/`partial` 或教师复核保存后，会原子刷新同一文件；
- 未配置 AI 时仍会保留受约束的源码快照和行为事件，不会伪造 AI 结论；
- 会话目录可能含有学生代码和教学数据，必须按单位的数据留存与访问政策处理。

自动记录与文件夹入口的验证资料见 [训练记录与日志文件夹验证记录](docs/2026-08-03-automatic-training-record-folder-verification.md)。

## 9. 验证记录

以下包含本轮 P0 修复后于 2026-08-04 重新执行的结果：

| 范围 | 本轮结果 | 说明 |
| --- | --- | --- |
| P0 后端 RED/GREEN | `5 failed` → `5 passed` | 首次截断、二次上限、非截断不重试、测试建议与后台 attempt |
| 后端关联回归 | `137 passed` | 共享传输、测试建议和分析 worker |
| 后端全量测试 | `604 passed` | 在允许 pytest-jupyter 绑定临时本机端口后通过 |
| P0 前端 RED/GREEN | 结果区 `3 failed` → `18 passed`；验收补测 `1 failed` → `1 passed` | AI 失败空状态、侧栏失败文案、ordinary partial 和有效 `not_observed` |
| 前端关联回归 | 原结果视图与侧栏联动 `93 passed`，另增 1 项验收回归 | 最终全量覆盖新测试 |
| 最终审查 RED/GREEN | `8 failed` → `77 passed, 7 subtests passed` | 覆盖 ready/日志竞态、符号链接替换、非 AI 日志冻结、OpenAPI 与 Demo 强校验 |
| 0.2.1 后端全量 | `634 passed, 23 subtests passed` | 包含日志生成、安全分块下载、API/Schema、Demo 和 wheel 制品门禁 |
| 0.2.1 前端全量 | `279 passed` | 20 个 Jest 套件，包含三项日志状态、有限轮询、并发查看器、动态前缀和插件接线 |
| Demo 自动验证 | `16 passed, 23 subtests passed` | 无付费 AI，交叉校验三份日志会话、状态、事件、结构与白名单导出 |
| lint 与生产构建 | 退出 0 | Prettier/ESLint/stylelint 与 JupyterLab prebuilt 生产构建通过 |
| wheel 内容与完整性 | `OK`、`Done testing` | `myextension-0.2.1-py3-none-any.whl`，SHA-256 已回填 |
| 隔离 wheel 安装 | 前后端 `enabled / OK` | JupyterLab 4.6.1、Server 2.20.0，安装版本 0.2.1 |
| 隔离会话日志烟测 | `ISOLATED_SESSION_LOG_SMOKE=PASS` | finalize 前两项 ready，模拟 ready 分析后三项 ready，可读取完整内容 |
| 隔离 wheel 浏览器验收 | 两项 P0 均通过 | 合成 provider、隔离端口、无真实/付费 AI，现有 8899 未触碰 |

0.2.1 的核心复现命令：

```bash
.venv/bin/python -m pytest -q
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm test --runInBand
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm lint:check
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm build:prod
uv build --wheel --offline
.venv/bin/check-wheel-contents dist/myextension-0.2.1-py3-none-any.whl
.venv/bin/python -m zipfile -t dist/myextension-0.2.1-py3-none-any.whl
shasum -a 256 dist/myextension-0.2.1-py3-none-any.whl
```

构建使用本机 uv 缓存的离线依赖；验证没有调用真实/付费 AI，没有部署、推送镜像或修改当前 `127.0.0.1:8899` 预览。

当前 wheel 的 SHA-256 已在本轮重新核对。完整命令、RED/GREEN 失败摘要、沙箱端口/离线 uv 缓存限制与未执行项见 [结构化 AI 截断恢复与失败态验证记录](docs/2026-08-04-structured-ai-truncation-recovery-verification.md)。

### 9.1 客户验收前 P0 实施状态

#### P0-1：结构化 AI 首次请求偶发失败

现象：在“确认测试并发布”阶段，AI 测试建议第一次请求失败，第二次手工点击才成功。

已定位证据：首次请求约 58 秒后返回 502；相关会话分析响应出现 `finish_reason=length`、输出达到 `4096` token、最终 `content` 为空，说明结构化正文被截断或没有生成。

已批准设计：共享 `chat_json()` 首次明确使用 8192 输出预算；仅在 `finish_reason=length` 时以 16384 预算自动重试一次，并保留失败关闭和调用上限。详见 [结构化 AI 响应截断恢复设计](docs/superpowers/specs/2026-08-04-structured-ai-truncation-recovery-design.md)。

实施：共享 `chat_json()` 首次使用 8192 预算；只在 `finish_reason=length` 时以 16384 预算恢复一次；连续截断以稳定错误失败关闭，普通无效 JSON 不会产生第二次请求。

当前状态：**已实现、已通过隔离 wheel 浏览器中的首次截断自动恢复并进入最终 wheel；真实 AI 与目标安装环境仍待验收。**

#### P0-2：AI 分析失败时仍显示“0 条证据”卡片

现象：会话已经达到最低观察要求，但 AI 结构化响应失败后，界面仍显示三个“部分结果”卡片和“查看 0 条证据”。

根因：采集链路已有有效事件，但没有产生合法 `ai_result`；现有前端仍渲染分析失败后的占位维度结果，导致数据质量达标和 AI 分析成功两个状态被混淆。

实施：AI 分析失败且没有 `resolved` 维度或非空 `ai_result` 时，结果页显示行为采集完成、AI 分析未完成和已有数据质量原因，不渲染占位维度卡或复核表单；普通 partial 与有效 `not_observed` 保持可见，但无 claim 的维度不显示空证据控件。

当前状态：**已实现、已通过隔离 wheel 浏览器失败流程和兼容回归、已进入最终 wheel；真实 AI 与目标安装环境仍待验收。**

#### P0 验收门槛

两项修复完成后至少执行：

```bash
.venv/bin/python -m pytest -q myextension/tests
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm lint:check
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm test --runInBand
PATH="$PWD/.venv/bin:$PATH" .venv/bin/jlpm build:prod
uv build --wheel --offline
.venv/bin/check-wheel-contents dist/myextension-0.2.1-py3-none-any.whl
.venv/bin/python -m zipfile -t dist/myextension-0.2.1-py3-none-any.whl
```

以上自动化门槛已完成，包括合成客户端的“首次截断、第二次自动成功”。新 wheel 已在 `/private/tmp` 隔离 target 和独立 JupyterLab 中完成浏览器验收；当前 8899 预览未修改。真实 AI、真实学生数据和付费模型仍未用于验收。

### 9.2 Windows 真机待验收

Windows 启动脚本、路径处理和 Explorer 打开逻辑已经实现，但目前没有完成 Windows 真机上的最终验收。客户交付前应在真实 Windows 10/11 机器验证：

- wheel 安装和 JupyterLab 启动；
- 前后端扩展均启用；
- 从创建方案到生成训练记录的完整流程；
- “本次日志”三项在页内的查看、下载和状态转换；
- 高级诊断中的“打开日志文件夹”可选验证 Explorer；
- 中文路径、空格路径和普通用户权限场景；
- 停止、重启和卸载行为。

## 10. 安全与数据处理要求

交付或演示时禁止共享以下内容：

- `log/`、`sessions/`、`jobs/` 中的真实数据；
- 包含学生源码、错误文本或行为时间线的文件；
- `.ark_ai_config.json` 或其他 AI 配置文件；
- API Key、Jupyter token URL、Cookie 或认证信息；
- 未经授权的真实学生数据。

关键要求：

- 保留 Jupyter token 或密码认证，不要为了演示关闭认证；
- API Key 只保存在 Jupyter Server 所在机器，支持的平台应使用 `0600` 私有权限；
- BLUEDOT 单用户试点页面保存的 Key 可落在 `/workspace/code/.behavior-audit/.ark_ai_config.json`；该文件同样禁止交付、下载分发或提交；
- 外部 AI 只接收完成分析所必需的受约束候选内容；
- 不把真实日志提交到版本库、聊天或共享网盘；
- 删除会话前应按单位数据政策完成授权、备份或留存判断。

## 11. 正式部署前仍需完成的工作

当前版本定位是本地单用户 Pilot。若要正式面向客户部署，至少需要：

1. 明确部署形态：教师本机单用户，或远程服务器/多用户平台；
2. 在目标环境安装当前 0.2.1 wheel，完成真实 AI 与目标环境失败恢复回归；
3. 完成 Windows 真机和目标 macOS 环境验收；
4. 若为远程或多用户场景，补充身份认证、权限、租户隔离和管理员审计；
5. 配置 HTTPS、域名、反向代理和受控的密钥管理；
6. 确定教学数据的保存位置、访问范围、保留周期、备份和删除政策；
7. 建立与本地一致的 CI 质量门禁和可复现构建流程；
8. 增加负载、稳定性、异常恢复和升级/回滚验证；
9. 完成隐私、数据合规和外部模型使用审查；
10. 制定运行监控、故障响应、版本发布和客户支持流程。

在这些工作完成前，不应将当前 Pilot 描述为已具备 JupyterHub 多租户生产能力。

## 12. 回滚、卸载与数据保留

软件卸载：

```bash
jupyter server extension disable myextension --sys-prefix
python -m pip uninstall myextension
```

若 0.2.1 安装后需要回退，可卸载后安装保留的 0.2.0 或 0.1.0 wheel。回滚软件不会自动删除现有会话数据；会话、训练记录和教师复核数据应单独按数据政策处理。

## 13. 交接检查清单

- [ ] 接收方已取得项目目录与 0.2.1 wheel；
- [ ] 接收方已核对 wheel SHA-256；
- [ ] 接收方已阅读 `README.md`、`启动说明.md` 和 `项目说明.md`；
- [ ] 已在目标机器验证前后端扩展启用；
- [ ] 已完成不含真实学生数据的完整演示；
- [ ] 已确认三份“本次日志”可直接查看/下载，且持久化位置符合数据政策；
- [ ] 已确认 API Key、Jupyter token 和会话数据未进入交付截图或共享文件；
- [x] 已明确两项 P0 已进入当前 wheel 并通过隔离安装预览，但真实 AI、目标环境与 Windows 验收仍未完成；
- [x] P0 修复后已重新运行全量测试、构建 wheel 并更新 SHA-256；
- [x] 0.2.1 三份本次日志、动态前缀 API、全量回归、离线 wheel 与隔离安装已验证；
- [ ] 已完成 Windows 真机验收；
- [ ] 若进入正式部署，已另行完成生产部署与数据合规评审。

## 14. 当前开发预览说明

本机开发预览当前运行在 `127.0.0.1:8899`。访问时请使用启动终端输出的完整 token URL；不要把含 token 的地址写入交付文档、截图或对外消息。

本交接文档完成后不自动停止预览服务、不部署、不推送、不调用真实外部 AI，也不修改现有会话数据。
