# 项目说明

## 项目定位

本项目是面向编程教学的 JupyterLab 行为证据采集与分析 Pilot。它记录学生在
Notebook 和 Python 文件中的客观过程事件，并根据教师为具体题目发布的观察维度，
在一次完整会话结束后生成可核查、可复核的教学观察。

核心价值不是自动给学生贴通用标签，而是让教师从一道具体题目出发，确认：

- 这道题希望考察哪些知识点；
- 哪些测试草稿可以帮助教师检查题目覆盖；
- 每个知识点应观察哪些可核查的编程行为；
- 哪些表现支持该观察，哪些情况应排除。

## 当前 0.2.1 流程

```text
教师输入题目和可选的考察重点
→ AI 推荐知识点，教师采用、修改或自行添加
→ 教师确认知识点
→ AI 生成测试草稿，教师编辑并确认
→ 发布不可变 Profile v2 pilot 方案
→ 学生选择该方案并确认本次采集用途
→ 开始监控
→ 本地分批、幂等上传行为事件
→ 停止监控并校验完整连续序号
→ 为完整会话创建一个持久化分析任务
→ 停止后立即生成 operation_log.json 和 process_log.md
→ AI ready 后生成 analysis_log.json
→ 在左侧“本次日志”直接查看或下载
→ 已配置 AI：AI 只判断方案中的教师维度，并显示结果卡、证据和教学建议
→ 未配置 AI：只显示“数据采集完成，尚未进行 AI 分析”
→ AI 分析 ready/partial 或教师复核后自动刷新同一训练记录
```

Profile v1 继续可读、可选并可用于旧会话；新建题目方案使用 Profile v2。当前测试
仅作为教师确认题目覆盖范围的方案定义，不执行，也不直接生成“已掌握/未掌握”
结论。

监控默认关闭。每次会话都需要选择一个已发布 `pilot` 方案并重新确认；停止前不会
触发整段 AI 分析。

## 题目驱动的知识点方案

教师使用引导式三步界面：

1. 输入完整题目；可选填希望考察的知识点，一行一个。方案名称、题目 ID、答题
   方式和函数入口放在折叠的“高级设置”中。
2. 检查 AI 知识点候选。候选与已采用内容分开，教师可以采用、忽略、编辑、删除、
   排序或手工添加。
3. 确认知识点后检查测试草稿，编辑输入、期望结果、关联知识点和启用状态；确保
   每个知识点至少被一个启用测试覆盖后确认并发布。

AI 推荐只是候选，未经教师采用和确认不会进入发布版本；AI 不可用时仍可完全手工
完成。修改题目、知识点或排序会保留可复用的测试草稿，但使知识点和测试两级确认
失效；删除知识点只移除相关引用及没有任何剩余引用的孤儿测试。修改测试只使测试
确认失效。

Profile v2 保存题目上下文、知识点、测试定义、教师确认哈希，以及每个知识点绑定
的行为观察维度。系统维护底层信号、最低观察量、提示词、等级代码和 Schema。
普通教师无需编辑阈值或 JSON。当前行为等级固定为“可能出现”和“明显出现”。
未观察到、数据不足、无法计算、部分结果和分析失败与等级分开保存，不会用伪造
等级填空。

发布版本不可变。会话保存确切的 `profile_id`、版本和内容哈希，保证结果与当时的
教师定义一致。

## 采集与可靠上传

插件可采集以下客观事件：

- 代码输入、删除和粘贴；
- 运行开始、成功、失败和错误类型；
- 动作间停顿、页面离开/返回、单元格或文件切换；
- 事件发生时间、会话连续序号和必要的文档/单元格上下文。

前端把事件形成连续批次；服务端使用批次 ID、内容哈希和连续序号实现幂等接收。
同一批次重放不会生成重复事件。停止时先排空剩余队列，再 finalize；只有 exact
finalize 才创建一个会话级分析任务。浏览器中断留下的未完成会话必须显式恢复或
放弃，不能静默覆盖。

侧栏的“有效观察时长（证据覆盖）”统计代码输入、删除、粘贴及页面活动时的动作间停顿。页面离开和运行耗时不计入，但运行事件会进入日志。达到门槛只表示行为证据覆盖足够，与日志生成或 AI 分析等待无关。

## 分析结果

普通界面按会话显示：

- 每个教师维度的普通语言结论；
- 结论说明；
- 可展开的事件证据；
- 方案中绑定的教学建议；
- 数据不足、部分结果或失败时的可操作提示；
- 教师复核入口。

“数据已采集”和“AI 已给出结论”是两个独立状态。若
`error_code=ai_not_configured`，普通界面不渲染维度结论卡或教师复核表单，避免
把最低观察量已满足误读为已有行为结论。配置 AI 服务后可对保留的完整会话重试
分析。

技术来源信息默认折叠；provider 请求 ID、哈希、绝对路径、原始事件代码和内部
数值支持度不进入普通结果卡。原始 AI 结果是不可变制品，教师复核以追加记录覆盖
展示，不改写原文件。

历史兼容文件仍可从“高级数据”打开，但它们不是普通用户的主流程，也不存在
“全局最新结果”作为当前结论。

## 本次日志与自动记录

普通侧栏固定显示 `operation_log.json`、`process_log.md`、`analysis_log.json` 三项。前两份在会话 finalize 后立即生成，第三份在 AI 分析真实 `ready` 后生成；等待和失败状态均不会伪造可查看文件。文件名、“查看”和“下载”都在 JupyterLab 页面内工作；“打开日志文件夹”只保留在“高级数据”中作本机诊断。

停止监控后，服务端同时继续维护 `sessions/<session_id>/training_record.json`。AI 分析进入
`ready`/`partial` 或教师复核成功保存后，服务端刷新同一文件。未配置外部 AI 时记录仍会
生成，刚停止时 `ai_analysis` 可以为空；后台检查后，最低观察量不足会写入不调用外部模型的
coverage-only `ready` 结果；需要模型但未配置时，会话分析状态变为 `partial`，训练记录随状态
刷新，但公开 `ai_analysis` 仍为空，不会伪造模型结论。受约束的历史源码快照与行为事件继续
保留。

平台内查看/下载经过 Jupyter 认证 API，不依赖 Notebook 根目录或客户端文件管理器。高级诊断中的文件夹操作仍只影响运行 Jupyter Server 的机器。

训练记录是从权威会话工件生成的派生文件，不包含凭据、prompt 正文、provider 原始响应或本机
绝对路径。真实日志、会话目录和训练记录可能包含学生代码，不得提交到版本库或发送到外部服务、
聊天或共享位置。

## 数据工件

服务端数据根目录包含当前权威会话工件以及兼容投影。主要类型如下：

| 类型                     | 作用                                          |
| ------------------------ | --------------------------------------------- |
| 会话快照                 | 绑定题目、方案版本、信号字典和生命周期状态    |
| 批次与回执               | 支持幂等重放和连续序号验证                    |
| `raw_events.jsonl`       | 当前会话的权威原始事件流                      |
| 分析 job/attempt         | 持久化排队、运行、重试和终态                  |
| prompt/raw response 快照 | 服务端私有审计工件                            |
| `result.json`            | 不可变的结构化 AI 结果                        |
| review history           | 追加式教师确认/修正                           |
| `training_record.json`   | 停止监控后自动生成并随分析/复核刷新的派生记录 |
| Markdown/时间线等        | 兼容旧版查看与研究导出                        |

删除会话必须输入精确会话 ID。成功后级联删除该会话、任务、分析、私有快照、
复核和兼容投影，并保留不含内容的删除审计记录。

## AI 与隐私边界

- 数据默认保存在运行 Jupyter Server 的机器；项目不主动上传数据库。
- 只有配置外部 provider 并在停止后分析时，必要的脱敏候选片段才可能发送到该
  provider。
- API Key 只在 Server 机器保存；支持时权限为 `0600`，不得提交或共享。
- UI 提供带二次确认的“清除已保存 Key”，用于停止使用当前 provider。
- 旧版本保存过的 credential 在迁移后应轮换。
- 本机绝对路径、Key 和显式身份字段不会进入持久化提示来源或错误响应。
- 学生代码、注释、输出和错误文本均视为不可信数据；提示注入文本会在 prompt
  边界处理，模型结果还必须通过维度、等级和真实 event/criterion 引用校验。

## 适用边界

这是本地单用户 Pilot，不是评分或诊断系统：

- 结果不得用于自动成绩、处分、排名或高风险决策；
- 不推断稳定能力、知识掌握、人格或心理状态；
- 运行成功不能证明答案正确，停顿不能证明犹豫；
- 模型自评不是正确概率；
- 当前没有 JupyterHub 多租户授权、正式标注一致性、盲测或
  `pilot → approved` 校准流程。

若要进入正式教学研究或多用户部署，需要另行完成数据治理、权限隔离、保留策略、
人工标注协议、校准集/锁定盲测和统计验证。
